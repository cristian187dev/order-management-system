
# Script de base de datos

El archivo `bd/oms_db_schema.sql` contiene el script para crear la base de datos `oms_db` utilizada por el sistema Order Management System.

## Instrucciones

1. Abrir MySQL Workbench o cualquier cliente SQL.
2. Ejecutar el script `bd/oms_db_schema.sql`.
3. Asegurarse de que la base de datos `oms_db` quede creada correctamente antes de iniciar la aplicación.

